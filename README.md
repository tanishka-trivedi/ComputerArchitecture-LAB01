This project implements a minimal CPU datapath using Verilog. Although it supports only a small set of arithmetic and logical operations, it demonstrates the fundamental flow of a processor:

Fetch → Decode → Execute

The assignment provides a foundation for extending the processor with additional instructions, registers, a program counter, control logic, and more advanced CPU functionality.
